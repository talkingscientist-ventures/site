# Friction log — site repo

| Date | Role | Category | Note |
|---|---|---|---|
| 2026-08-24 | site-build | technical | mediabeast runs Node 18; Playwright ≥1.60 requires Node 20+, so browser verification had to run on tsv01 instead of the build machine. Consider upgrading mediabeast Node or standardizing verification on tsv01. |
| 2026-08-24 | site-build | ignorance | tsv01's global Playwright (/usr/lib/node_modules) is not importable by ESM scripts via NODE_PATH; workaround is `ln -sfn /usr/lib/node_modules /tmp/node_modules` next to the script. Worth a note in tsv01's RUNNING.md. |
| 2026-08-24 | site-build | technical | rsync --delete removed DreamHost's root-owned `.dh-diag` symlink from the docroot along with the placeholder files. Harmless (panel diagnostic link), but deploy.sh mirrors exactly — anything DreamHost drops into the docroot will be deleted on next deploy. |
| 2026-08-24 | site-build | ignorance | New repo clones on mediabeast must blank the credential-helper list before adding the tsv-claude PAT helper (`helper = ""` then the PAT line, as tsv-ops does) — otherwise the global gh helper answers first with the wrong account and pushes fail "Repository not found". |
| 2026-08-24 | design-v2 | technical | Playwright CAN run on mediabeast after all: nvm node 22 + borrowing an existing install's node_modules (repos/agent-office has playwright 1.62.0, matching the cached chromium-1234 in ~/.cache/ms-playwright). Symlink that node_modules next to the script; no tsv01 hop needed. ccflow's playwright 1.61.1 does NOT match the browser cache. |
| 2026-08-24 | design-v2 | technical | file:// screenshots break on root-relative asset paths (/assets/style.css); serve the repo with `python3 -m http.server` for local checks instead. |
| 2026-08-24 | design-v2 | process | REVIEW-CODEX.md dropped into the repo root during the redesign was briefly deployed publicly: deploy.sh excluded only .git/ops/deploy.sh/README.md, and rsync --delete does not remove already-staged excluded files without --delete-excluded. Fixed: EXCLUDES now covers *.md, staging rsync uses --delete-excluded; live URL verified 404. Keep internal docs in ops/, never the repo root. |
| 2026-08-24 | content-fixes | technical | Worker sessions launched with cwd=repos/talkingscientist-ventures get a bash sandbox whose write allowlist covers only that repo — every git/file operation in repos/site needed a per-command sandbox override. Launch site workers with cwd=repos/site (or widen the sandbox allowlist) to avoid this. |
| 2026-08-24 | content-fixes | process | Live CSP verification nuance: a hard-404 URL always logs one "Failed to load resource: 404" console error for the page request itself. That is the 404 status, not a CSP or asset failure — automated checks should whitelist it. Google Fonts load fine under the F9 CSP on every page including the 404 page. |
